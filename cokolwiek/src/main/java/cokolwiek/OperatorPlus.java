package cokolwiek;

public class OperatorPlus implements StringConcentrator {

    public String concatenate(String first, String second) {
        String newString;
        return newString = first + " " + second;
    }
}
